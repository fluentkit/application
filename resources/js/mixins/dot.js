import { dotSet, dotGet } from '../utils/dot';

export default {
    methods: {
        dotSet (...args) {
            return dotSet(...args);
        },
        dotGet (...args) {
            return dotGet(...args);
        }
    }
}
